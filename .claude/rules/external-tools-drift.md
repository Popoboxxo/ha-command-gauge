# External-Tool Injection Drift

> Automatisch erkannt von `check_injection_drift` — Fremd-Artefakte, die keinem aktiven Tool in `config/plugin-catalog.yaml` als `permitted-injections` deklariert sind. Nur Warnung, kein automatisches Eingreifen.

---

- `.claude/agent-meta-state.json` (other) — keinem registrierten Tool zugeordnet
- `.claude/platform-config.yaml` (other) — keinem registrierten Tool zugeordnet

